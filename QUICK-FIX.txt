═══════════════════════════════════════════════════════════════════
  PRIORITY RULES DUPLICATION - QUICK FIX GUIDE
═══════════════════════════════════════════════════════════════════

✅ DATABASE CLEANED: 3 duplicate rules removed

🔴 ROOT CAUSE: You're accessing the WRONG APPLICATION on port 3000

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 IMMEDIATE FIX:

   Access the CORRECT URL: http://localhost:3001

   Your React dev server is on PORT 3001, not 3000!
   Port 3000 is serving Excalidraw POC (wrong app with old code)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📋 STEP-BY-STEP:

1. Close browser tabs for localhost:3000
2. Open NEW tab: http://localhost:3001
3. Go to Settings → Priority Rules
4. Create a test rule
5. Check Go server logs for: 🔄 [DEBOUNCE]

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔧 OPTIONAL: Reclaim Port 3000

Terminal 1:
   kill 50366
   (or: lsof -i :3000 | grep LISTEN | awk '{print $2}' | xargs kill)

Terminal 2 (in project directory):
   npm start

Then access: http://localhost:3000

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🧹 CLEAR BROWSER CACHE (Recommended):

Chrome/Edge:
   F12 → Right-click refresh → "Empty Cache and Hard Reload"

Firefox:
   Ctrl+Shift+Delete → Cache only → Everything → Clear Now

Safari:
   ⌥⌘E (Option+Command+E)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✅ VERIFICATION:

After fix, creating a new priority rule should:
   • Create ONLY 1 rule in database (not 3)
   • Show 🔄 [DEBOUNCE] in Go server logs
   • Save after 500ms delay (not immediately)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🔍 DEBUG COMMANDS:

Check what's on each port:
   lsof -i :3000 -n -P
   lsof -i :3001 -n -P

Verify debouncing code in bundle:
   curl -s "http://localhost:3001/static/js/bundle.js" | grep DEBOUNCE

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📖 Full details: See BROWSER-CACHE-FIX.md

═══════════════════════════════════════════════════════════════════
